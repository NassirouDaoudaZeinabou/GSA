<section class="agents-edit admin-dashboard">
  <header class="dashboard-header">
    <h1>Modifier l'agent</h1>
    <p class="muted">Mettre à jour les informations de l'agent</p>
  </header>

  <div style="margin-top:1rem">
    <%= render 'form', client: @client, agent: @agent %>
  </div>
</section>
