<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Gsa" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
  </head>

  <body>
    <header class="app-topbar" style="display:flex;align-items:center;padding:.5rem 1rem;background:#fff;border-bottom:1px solid #eee;">
      <div style="flex:1">
        <a href="/" style="text-decoration:none;color:var(--green-700);font-weight:700">GSA</a>
      </div>
      
    </header>

    <%# Render sidebar only for admin namespace pages %>
    <% sidebar_present = controller_path.start_with?('admin/') %>
    <% if sidebar_present %>
      <%= render 'shared/sidebar' %>
    <% end %>

    <main class="main-content <%= 'with-sidebar' if sidebar_present %>" role="main">
      <%= yield %>
    </main>
  </body>
</html>
